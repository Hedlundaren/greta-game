import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  gestureContainer: {
    width: '100%',
    height: '100%',
    flex: 1,
  },
  graphicsContainer: {
    width: '100%',
    height: '100%',
    flex: 1,
  },
})